import styled from "styled-components";
import Modal from "react-modal";

const StyledModalAnnouncement = styled(Modal)``;

const StyledModalContainer = styled.div`
  .modal-overlay {
    background-color: blue;
  }
`;

export { StyledModalContainer };
