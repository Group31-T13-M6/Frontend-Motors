import styled from "styled-components";

const StyledButtonClose = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;

  button {
    color: var(--grey4);
    background: none;
    font-size: 18px;
  }
`;

export { StyledButtonClose };
